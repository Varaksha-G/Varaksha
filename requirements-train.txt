# requirements-train.txt
# ─────────────────────────────────────────────────────────────────────────────
# TRAINING ONLY — run locally, never installed on the server.
# Usage: pip install -r requirements-train.txt
#        python services/local_engine/train_ensemble.py
#        → emits data/models/*.onnx  (commit these or upload to model registry)
# ─────────────────────────────────────────────────────────────────────────────

# Core ML
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0
xgboost>=2.0.0
lightgbm>=4.3.0
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.1.0

# ONNX export
skl2onnx>=1.16.0
onnxmltools>=1.12.0     # XGBoost → ONNX converter
onnx>=1.16.0
onnxruntime>=1.18.0     # smoke-check exported models locally

# Dev
pytest>=8.0.0
